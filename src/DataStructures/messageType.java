package DataStructures;

/**
 * A class representing a type of a message, this is later used for the message data class
 */

public enum messageType {
    TEXT,
    LOGIN,
    LOGOUT,
    ACTIVE,
    NAME_TAKEN,
    LOGGED_IN,
    WRONG_FORMAT,
    MESSAGE_OK,
    TARGET_NOT_FOUND
}
